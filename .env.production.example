# ===========================================
# CONFIGURATION PRODUCTION - CRM TPM FINANCE
# ===========================================
# Copier ce fichier en .env.production et remplir avec les vraies valeurs

# --- BASE DE DONNÉES POSTGRESQL ---
POSTGRES_USER=crm_user
POSTGRES_PASSWORD=GENERER_MOT_DE_PASSE_FORT_ICI
POSTGRES_DB=crm_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# URL complète de connexion (utilisée par le backend)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# --- BACKEND API (FastAPI) ---
# Mode debug (TOUJOURS False en production)
DEBUG=False

# Clé secrète pour JWT (GÉNÉRER UNE CLÉ FORTE!)
# Générer avec: openssl rand -hex 32
SECRET_KEY=GENERER_CLE_SECRETE_ICI

# Origines CORS autorisées (format JSON array)
# En production, autoriser uniquement le domaine HTTPS
ALLOWED_ORIGINS=["https://crm.alforis.fr"]

# Port d'exposition de l'API (interne au serveur)
API_PORT=8000

# --- FRONTEND (Next.js) ---
# URL de l'API backend (accessible depuis le navigateur)
# En production avec Nginx, utiliser le chemin relatif ou l'URL complète
NEXT_PUBLIC_API_URL=https://crm.alforis.fr/api/v1

# Port d'exposition du frontend (interne au serveur)
FRONTEND_PORT=3010

# --- DOCKER ---
# Port externe pour PostgreSQL (laisser vide ou 127.0.0.1:5432 en production)
POSTGRES_EXTERNAL_PORT=5432

# --- SÉCURITÉ JWT ---
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# --- UPLOADS & LIMITES ---
MAX_UPLOAD_SIZE_MB=10

# --- ENVIRONNEMENT ---
ENVIRONMENT=production
