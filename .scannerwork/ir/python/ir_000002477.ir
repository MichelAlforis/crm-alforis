
‡ý"
    DÃ©corateur pour exiger un rÃ´le minimum sur une route

    Usage:
        @router.get("/admin/settings")
        @require_role(UserRole.MANAGER)
        async def get_settings(current_user: User = Depends(get_current_user)):
            ...

    Args:
        min_role: RÃ´le minimum requis

    Raises:
        HTTPException: 403 si l'utilisateur n'a pas le bon rÃ´le
    "strmin_role
£ £(#"ß#
!

Ã Ã(	"#unknown-value#)
'

³ ³(-"#unknown-value#BtypeC
A

³ ³(-	min_level""
models.role.Roleget_role_level:*
(

µ Ã(%closure"#new-object#-
+

µ Ã("#set-field# min_level:,
*

µ Ã("#set-field# min_role:-
+

µ Ã("?python-partial-apply?:

Ã Ã(	*P/Users/test/Documents/ALFORIS FINANCE/06. CRM/V1/crm-backend/core/permissions.py