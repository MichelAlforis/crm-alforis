# ============================================================
# Configuration Email pour Module Campagnes (Chapitre 6)
# ============================================================
# Copier ce fichier dans .env et renseigner les valeurs

# Resend Configuration (RECOMMANDÉ)
# Obtenir une clé API sur : https://resend.com/api-keys
# Simple, moderne, 100 emails/jour gratuit, 3000 emails/mois à partir de $20
RESEND_API_KEY=

# OU SendGrid Configuration
# Obtenir une clé API sur : https://app.sendgrid.com/settings/api_keys
# 100 emails/jour gratuit
SENDGRID_API_KEY=
SENDGRID_EVENT_WEBHOOK_KEY=

# OU Mailgun Configuration
# Obtenir une clé API sur : https://app.mailgun.com/app/account/security/api_keys
# 100 emails/jour gratuit les 3 premiers mois
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# Configuration Email par défaut
DEFAULT_EMAIL_FROM_NAME=Alforis CRM
DEFAULT_EMAIL_FROM_ADDRESS=noreply@example.com
DEFAULT_EMAIL_REPLY_TO=support@example.com
DEFAULT_EMAIL_UNSUBSCRIBE_BASE_URL=https://example.com/email/unsubscribe

# Limites d'envoi
EMAIL_RATE_LIMIT_PER_MINUTE=120
EMAIL_BATCH_SIZE=500

# Tracking
EMAIL_TRACK_OPENS=true
EMAIL_TRACK_CLICKS=true

# ============================================================
# Instructions
# ============================================================
#
# 1. Choisir un provider email (Resend RECOMMANDÉ, ou SendGrid, ou Mailgun)
# 2. Créer un compte et obtenir une clé API
# 3. Renseigner la clé API ci-dessus
# 4. Dans l'interface web, aller dans Paramètres > APIs Email
# 5. Configurer le provider et la clé API (cryptée en base de données)
# 6. Tester l'envoi d'email depuis l'interface
#
# Providers disponibles :
# - Resend (RECOMMANDÉ) : Moderne, simple, 100 emails/jour gratuit
#   https://resend.com/pricing
# - SendGrid : Mature, 100 emails/jour gratuit
#   https://sendgrid.com/pricing/
# - Mailgun : Puissant, 100 emails/jour gratuit (3 premiers mois)
#   https://www.mailgun.com/pricing/
#
# Note : La clé API sera également configurable via l'interface
# web (Paramètres > APIs Email) et sera cryptée en base de données
#
